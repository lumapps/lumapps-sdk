version: 2.1

jobs:
  python:
    parameters:
      tag:
        type: string
        default: latest
    docker: true
      - image: circleci/python:<< parameters.tag >>
    steps:
      - restore_cache:
          key: python<< parameters.tag >>-requirements-{{ checksum "requirements.txt" }}
      - run: pip install -e .
      - save_cache:
          key: python<< parameters.tag >>-requirements-{{ checksum "requirements.txt" }}
          paths:
            - "~/.cache/pip"
            - "/usr/local/lib/python<< parameters.tag >>/site-packges"
            - "/usr/local/lib/site-python"


  workflows:
    build:
      jobs:
        - python:
            tag: "2.7"


          